# Go
backend/bin/
backend/tmp/

# Node
frontend/node_modules/
frontend/dist/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.production

# Docker volumes
pg_data/
redis_data/
minio_data/
caddy_data/
caddy_config/
backend/sluggen

# macOS packaging output (build-app.sh)
macos/*.app/
macos/*.dmg
macos/*.zip
# Intermediate iconset folder is throwaway, but AppIcon.icns is tracked —
# it's the source-of-truth icon bundled into the .app. If it's not in the
# repo, build-app.sh silently produces an icon-less .app.
macos/AppIcon.iconset/
scripts/.qcheck.env
ai-wallpapers/
