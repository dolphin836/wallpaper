/* Placeholder — brand/system artboards from the previous iteration have been
   removed from the canvas while we focus on Discover + Detail.
   Re-add if needed; the tokens are still defined in tokens.css. */
window.__brand_jsx_loaded = true;
