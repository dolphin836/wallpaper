# Local dev hits the production API directly (backend CORS already
# allows any http://localhost:*). Only loaded by `npm run dev` —
# production builds are unaffected. Point this at a local backend by
# deleting the line (falls back to the /api proxy in vite.config.ts).
VITE_API_BASE_URL=https://wallpaperexchange.com/api/v1
